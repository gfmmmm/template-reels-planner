# 릴스대본기획 데이터 접속 정보.
# 이 파일을 복사해 같은 자리에 .env 로 저장하고 값을 채운다 (.env 는 커밋되지 않음).
# 값은 SETUP.md 2단계에서 Claude Code 가 대신 채워 준다 — 직접 편집할 필요 없음.

# (A) 크리에이터 대시보드(정적·JSON)를 쓰는 경우 — 대시보드 폴더 경로
DASHBOARD_DIR=

# (B) Supabase 기반 대시보드를 쓰는 경우 — 아래 셋
SUPABASE_URL=
SUPABASE_KEY=
ACCOUNT_HANDLE=
