# 구축 가이드 — Claude Code가 읽고 사용자를 이끄는 대본

이 문서는 사람이 읽는 설명서이면서, 동시에 이 폴더에서 실행된 Claude Code가 그대로 따라가는 실행 지시서다.
사용자가 "구축 가이드대로 진행해줘", "세팅해줘", "처음이에요"라고 하면 Claude Code는 이 문서를 열고 0단계부터 한 단계씩 진행한다.

## Claude Code가 지킬 원칙 (전 단계 공통)

1. 터미널은 Claude가, 브라우저는 사용자가. 사용자에게 명령어를 치게 하거나 파일을 열어 고치게 하지 않는다. 키나 경로는 채팅에 붙이면 Claude가 파일에 쓴다.
2. 한 메시지에 행동 하나. 단계 끝은 항상 "다 되셨으면 '됐어요'라고 답해주세요" 한 문장으로 끝낸다. 두 가지를 동시에 시키지 않는다.
3. 확인 전에는 다음 단계로 가지 않는다. 단계마다 아래 "검증" 항목을 기계로 확인한다.
4. 왜 하는지 한 줄. 설명은 한 줄이면 충분하다. 길면 안 읽는다.
5. 진행 상태를 `.setup-progress.json`에 기록한다. 창을 닫고 나중에 "이어서 해줘"라고 하면 거기서 다시 시작한다.
6. 사용자를 부르는 호칭은 쓰지 않는다. 존댓말. 전문 용어는 괄호로 풀어 쓴다 — 예: API 키(서비스를 쓰게 해주는 열쇠).
7. `.env`의 값은 채팅에 다시 출력하지 않는다.

진행 파일 형식:

```json
{ "step": 2, "done": [0, 1], "handle": "", "mode": "json | supabase | manual", "updatedAt": "YYYY-MM-DD" }
```

---

## 0단계 · 시작 확인 (1분)

Claude가 말없이 확인한다.

- 현재 폴더에 `.claude/skills/릴스대본기획/SKILL.md`가 있는가 (없으면 "이 폴더가 아니에요. 다운로드한 폴더에서 다시 열어주세요")
- `python3 --version`, `curl --version`이 나오는가
  - Mac에서 python3가 없으면: `xcode-select --install`을 Claude가 실행하고 "팝업에서 설치를 눌러주세요, 5분쯤 걸려요"
  - Windows는 Git for Windows가 있어야 한다(Claude Code 요구사항). python이 없으면 https://www.python.org/downloads/ 에서 설치 안내(설치 화면 첫 페이지의 "Add python.exe to PATH" 체크 필수)
- `.setup-progress.json`이 있으면 읽고 "지난번에 N단계까지 하셨네요. 이어서 갈게요"

검증: 위 셋 통과. 통과하면 1단계로.

사용자에게 할 말:

> 릴스 대본 기획 도구를 세팅할게요. 다섯 단계, 넉넉히 20분이에요. 제가 컴퓨터 쪽은 다 할 테니 몇 가지만 답해주시면 됩니다.

---

## 1단계 · 데이터 연결 (3분)

이 도구는 본인 릴스 데이터와 참고할 릴스(레퍼런스) 데이터를 읽어서 대본을 짓는다. 데이터가 어디 있는지 정한다.

사용자에게 묻는다:

> 크리에이터 대시보드(본인 릴스와 레퍼런스가 모여 있는 화면)를 이미 만들어 두셨나요?
> 1) 네, 폴더가 있어요 → 그 폴더를 Finder(탐색기)에서 이 채팅창으로 끌어다 놓아주세요. 경로가 붙습니다.
> 2) 아니요, 아직 없어요 → 괜찮아요. 대본을 직접 붙여 넣는 방식으로 진행할게요.

답에 따라:

- 1) 폴더 경로가 오면 → 그 안에 `data/posts.json`이 있는지 확인. 있으면 `.env`에 `DASHBOARD_DIR=<경로>` 기록, mode=json. 없고 Supabase형 대시보드(`.env`에 SUPABASE_URL이 있는 폴더)면 그 세 값을 이 폴더 `.env`로 복사, mode=supabase.
- 2) → mode=manual. `.env`는 만들지 않는다.

검증:

- json: `data/posts.json`을 읽어 게시물 수와 핸들을 말해준다 — "게시물 N건, @핸들 계정이네요. 맞나요?"
- supabase: `bash .claude/skills/릴스대본기획/lib/fetch.sh ctx` 실행해 계정 정보가 나오는지
- manual: 건너뜀


---

## 2단계 · 계정 폴더 만들기 (1분)

사용자에게 묻는다(1단계에서 핸들을 이미 알면 "@핸들로 만들까요?"로 확인만):

> 인스타그램 아이디(@ 뒤의 영문)를 알려주세요.

Claude가 한다: `계정/_template/`를 `계정/<핸들>/`로 복사. 진행 파일에 handle 기록.

검증: `계정/<핸들>/프로필.md`가 있고, `계정/` 안에 `_template` 말고 폴더가 하나뿐인지.

> 계정 폴더를 만들었어요. 지금은 빈 양식이고, 다음 단계에서 본인 릴스로 채워요.

---

## 3단계 · 계정 문서 채우기 (10분)

이 도구가 "나답게" 쓰려면 다섯 문서가 필요하다 — 프로필(누구를 위한 채널인가), DNA(말투), 형식(잘 된 릴스의 구조), 훅(첫 3초 유형), 안티패턴(하지 말 것). 재료는 본인 릴스 대본이다.

재료 모으기 (mode별):

- json / supabase: "대표대본 갱신"을 실행해 조회 상위 릴스(기본 상위 20건, 또는 평균의 2배 이상)의 대본·캡션을 `계정/<핸들>/대표대본/`에 파일로 만든다. 대시보드에 내 릴스 코칭이 있으면 안티패턴 B절 재료로 함께 읽는다.
- manual: 사용자에게 부탁한다.

  > 본인 릴스 중 잘 된 것 10~20개의 대본과 캡션이 필요해요. 두 방법이 있어요.
  > 1) 릴스 주소(URL)를 한 줄에 하나씩 붙여 주세요 — 대본추출 도구(https://github.com/gfmmmm/claude-video-transcript)가 설치돼 있으면 제가 음성을 글로 뽑아요.
  > 2) 인스타 앱에서 릴스의 캡션을 복사하고, 영상 말을 받아 적어 붙여 주세요. 한 릴스당 "조회수 / 캡션 / 대본" 세 덩어리면 충분해요.

  받은 것을 `대표대본/_index.md` 형식대로 파일로 만든다. 첫 3초 시각분석은 사용자에게 "이 릴스 첫 3초에 화면에 뭐가 보이나요?" 한 줄씩 묻는다(모르면 비워 둔다).

문서 초안 쓰기: `/계정세팅` 스킬을 실행한다. 이 스킬이 재료 모으기(위 mode별 절차)부터 문서 5개 작성, 아래 사용자 검토 세 가지까지 전부 맡는다 — 숫자는 `measure.py`가 재고, 모든 문장에 M-### 출처가 붙는다. 스킬이 끝나면 4단계로.

사용자 검토 세 가지 (Claude가 하나씩 묻는다):

1. `대표대본/_index.md` 표를 보여주고 "이 중에 '나답지 않다' 싶은 릴스가 있으면 번호를 말해주세요" → 해당 파일 이름 앞에 `제외_`
2. DNA.md 8절 "쓰지 않는 것"을 보여주고 "여기에 더할 말이 있나요? 내가 절대 안 쓰는 표현"
3. 안티패턴.md A절을 보여주고 "하지 말라고 못 박을 것이 있나요?"

검증: 다섯 문서에 빈 표가 없고, 대표대본이 10건 이상.

---

## 4단계 · 첫 기획 (5분)

> 준비 끝이에요. 한 번 써볼게요. `/릴스대본기획`이라고 입력해 주세요.

세 질문(레퍼런스 · 유형 · 느낌)이 나온다. manual 모드면 레퍼런스 자리에 참고할 릴스의 대본이나 주제 한 줄을 붙이면 된다.
훅 3개가 나오면 하나 고르게 하고, 대본과 촬영 표까지 한 번 완주한다. "저장"까지 해서 `기획/`에 파일이 생기는 걸 보여준다.

검증: `기획/YYYY-MM-DD_*.md`가 하나 생겼다.

---

## 5단계 · 키우는 법과 업데이트 (2분)

마지막으로 네 마디를 알려준다.

> 쓰면서 이렇게 말하면 도구가 자라요.
> - "이건 하지 마" → 다음부터 그 표현·구조를 안 씁니다
> - "이 훅 추가해" → 새 첫 3초 유형이 등록됩니다
> - "이 말투 쓰자" → 말투 문서에 들어갑니다
> - "대표대본 갱신" → 새로 잘 된 릴스를 재료에 추가합니다
>
> 도구가 새 버전이 나오면 "템플릿 업데이트 받아줘" 한 마디로 받으세요. 본인 계정 문서와 기획 파일은 그대로 두고 도구 부분만 바뀝니다.

진행 파일에 step=5, done=[0..5] 기록. 끝.

---

## 막힐 때

| 증상 | 원인 | Claude가 하는 것 |
|---|---|---|
| `fetch.sh` 실행 시 ❌ .env 가 없습니다 | 1단계에서 supabase 모드인데 `.env` 미작성 | 1단계로 돌아가 세 값을 다시 받는다 |
| `python3: command not found` | Mac 개발자 도구 미설치 / Windows PATH 누락 | 0단계 안내대로 설치, 터미널을 닫고 다시 열게 한다 |
| 계정 폴더가 둘 이상 | 이전 세팅 흔적 | 어느 계정을 쓸지 묻고, 안 쓰는 폴더는 `계정/_archive/`로 옮긴다(지우지 않는다) |
| 대표대본이 10건이 안 됨 | 릴스가 적은 계정 | 기준선을 낮춰 상위 10건까지 채운다. 5건 미만이면 그대로 진행하되 "재료가 적어 말투 문서가 얇아요"라고 알린다 |
| 스킬이 안 보임(`/릴스대본기획` 자동완성 없음) | 다른 폴더가 열려 있음 / 새 대화 필요 | VS Code에서 이 폴더가 열려 있는지 확인하고 Claude 패널에서 새 대화를 시작한다(터미널이면 이 폴더에서 `claude`) |
